apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-test-app.fullname" . }}
  labels:
    {{- include "my-test-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "my-test-app.selectorLabels" . | nindent 4 }}
